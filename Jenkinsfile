pipeline {  
    agent any  
        stages {  
       	    stage("git_checkout") {  
           	    steps {  
              	    echo "cloning is a  repository" 
              	    echo "repo cloned successfully"
		    echo "repo java file started"
              	    }  
         	    } 
        }
}
